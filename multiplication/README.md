Implement recursive function for multiplying two numbers:
multiply_recursively(a, b)

* what happens if a is negative?
* what happens if b is negative?
* what happens if they both negative?
* what happens if zero
